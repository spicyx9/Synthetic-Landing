# Flux 01 : newsletter
Périmètre : bloc newsletter du pied de page (section `#newsletter`, formulaire `data-newsletter-form` → POST /api/newsletter) présent sur les 26 pages de production ; code `newsletter.js`, `api/newsletter.js`, `scripts/site_layout.py` (générateur du pied de page, lignes 81-89 et 134), README.md section "Homepage newsletter", `tests/newsletter.test.cjs` ; lignes d'inventaire : actions.csv (form, input_email, button, input_text company_website sur 26 pages), emails.csv E02, fichiers.csv (newsletter.js, api/newsletter.js, tests/newsletter.test.cjs), tiers.csv.
Outils : Playwright 1.56.1 (Chromium 141 headless, WebKit, Firefox) lancé via browsers.mjs et le proxy ; @axe-core/playwright 4.13 (scope `#newsletter`) ; curl ; `node --test` sur une copie du repo dans le scratchpad ; ImageMagick (réduction des captures) ; Python 3.11 (BeautifulSoup pour la doc Resend, calcul de contraste WCAG). Scripts : scratchpad/tools/nl01-common.mjs, nl01-pages.mjs, nl01-submit.mjs, nl01-variants.mjs, nl01-nojs.mjs, nl01-simul.mjs, nl01-recapture.mjs ; sorties : scratchpad/flux01/pages.json, submit.json, variants.json, nojs.json, simul.json, recapture.json et journaux .log.
Début : 2026-09-19T19:35:03Z  Fin : 2026-09-19T20:13:04Z
Statut : COMPLET

Le bloc newsletter est présent, accessible et correctement câblé sur les 26 pages, mais il n'enregistre personne : les deux soumissions valides réelles ont reçu HTTP 503 (clé `RESEND_API_KEY` absente de l'environnement de production, seule branche 503 du code) et le visiteur lit "Impossible de vous inscrire pour le moment. Réessayez." sur toutes les pages. Aucune information RGPD n'accompagne le champ et les politiques de confidentialité FR et EN ne mentionnent pas ce traitement ; sans JavaScript, la soumission aboutit à une page JSON brute (415). Le dispositif n'envoie aucun email de confirmation (pas de double opt-in, réabonnement possible d'un contact désinscrit par n'importe qui) et ne transmet pas la langue à Resend.

| Verdict | Sévérité | Page(s) | Élément | Attendu | Constaté | Preuve | Correction exacte |
|---|---|---|---|---|---|---|---|
| TROMPEUR | BLOQUANT | 26 pages (formulaire newsletter, testé sur /index-fr) | Soumission valide, envoi réel n° 1 | 200 `{"ok":true}`, message "Inscription confirmée.", contact créé dans Resend | HTTP 503 `{"ok":false}` en 2,7 s ; message affiché "Impossible de vous inscrire pour le moment. Réessayez." ; réponse produite par le handler lui-même (en-têtes `cache-control: no-store` et `content-type: application/json; charset=utf-8` des lignes 3-4, aucun en-tête `x-vercel-error`) ; l'unique branche 503 est `api/newsletter.js` ligne 21 `if (!process.env.RESEND_API_KEY) return reply(503);` : la clé Resend n'est pas configurée en production. Le champ est affiché sur les 26 pages alors qu'aucune inscription n'est possible | Requête 2026-09-19T19:51:53.981Z POST https://www.syntheticswarm.ai/api/newsletter, corps `{"email":"contact+audit-nl-20260919T195150Z@syntheticswarm.ai","lang":"fr","company_website":""}` ; réponse 19:51:56.700Z 503, `x-vercel-id: iad1::iad1::cnrw6-1789847516058-3171777dfa27`, corps `{"ok":false}` (scratchpad/flux01/submit.json) ; captures docs/audit-total/screens/01/index-fr-1440-newsletter-avant.png, index-fr-1440-newsletter-pendant.png (bouton "Inscription…" désactivé, aria-busy=true), index-fr-1440-newsletter-apres.png (réelle, message sous le bord bas du fenêtrage, état DOM enregistré : status="Impossible de vous inscrire pour le moment. Réessayez.") et index-fr-1440-newsletter-apres-rejeu503.png (même réponse rejouée dans le navigateur pour le cadrage) | C1 (section Corrections détaillées) |
| NON TESTABLE | n/a | /index-fr | Double soumission, envoi réel n° 2 (même adresse sans recharger) | 200 `{"ok":true}` idempotent (PATCH du contact existant, POST sinon, repli 409/422) | La 2e requête part bien (drapeau busy libéré) et reçoit le même 503 en 3,3 s : l'idempotence ne peut pas être observée tant que la clé est absente (raison du NON TESTABLE). La logique idempotente est confirmée par le code (`api/newsletter.js` lignes 28-35) et par le test unitaire (`tests/newsletter.test.cjs` lignes 32-37) : `node --test tests/newsletter.test.cjs` sur la copie du repo : pass 5, fail 0 | Requête 2026-09-19T19:52:01.121Z, réponse 19:52:04.399Z 503 `x-vercel-id: iad1::iad1::bfp7p-1789847524215-b54e616e7a96` (submit.json) ; captures index-fr-1440-newsletter-double.png et index-fr-1440-newsletter-double-rejeu503.png ; sortie node --test : "# pass 5 / # fail 0" | Aucune ; retester après C1 |
| DÉGRADÉ | MAJEUR | 26 pages | Soumission sans JavaScript, envoi réel n° 3 (contexte javaScriptEnabled:false) | Inscription, ou page d'erreur lisible avec retour au site | Le formulaire poste nativement en `application/x-www-form-urlencoded` (corps `email=contact%2Baudit-nl-nojs-20260919T195522Z%40syntheticswarm.ai&company_website=`) ; le serveur répond 415 (`api/newsletter.js` ligne 6) et le visiteur atterrit sur https://www.syntheticswarm.ai/api/newsletter qui affiche le texte brut `{"ok":false}`, sans titre, sans message, sans lien de retour | Requête 2026-09-19T19:55:30.580Z (navigation), réponse 19:55:34.093Z 415 `x-vercel-id: iad1::iad1::q44t9-1789847733716-9bb2254bee65`, page finale : title vide, contentType application/json, texte `{"ok":false}` (scratchpad/flux01/nojs.json) ; capture docs/audit-total/screens/01/index-fr-1440-newsletter-sans-js-resultat.png | C2 |
| DÉGRADÉ | BLOQUANT | 26 pages, /confidentialite, /privacy | Information RGPD à la collecte de l'email | Mention sous le champ (finalité, désinscription, lien vers la politique de confidentialité) et paragraphe dédié dans la politique (finalité, base légale, durée, sous-traitant Resend, transfert hors UE, droits) : art. 13 RGPD, art. L34-5 CPCE | La section `#newsletter` ne contient aucun lien et aucune mention : texte intégral "RESTEZ INFORMÉ Recevez nos nouveautés produit et annonces importantes. S’inscrire" (EN : "STAY IN THE LOOP Get product updates and important announcements. Subscribe") sur 26/26 pages ; `confidentialite.html` et `privacy.html` ne contiennent le mot "newsletter" que dans leur propre pied de page (lignes 76-82 et 95) et ne citent ni Resend, ni sous-traitant, ni transfert, ni ce traitement | pages.json (section.links = [] et mentionRgpd = false sur 26 pages) ; `grep -n -i newsletter confidentialite.html privacy.html` : lignes 76, 77, 79, 81, 82, 95 uniquement ; `grep -i -c resend confidentialite.html privacy.html` : 0 et 0 ; captures index-fr-1440-newsletter.png, opposition-1440-newsletter.png | C3 |
| DÉGRADÉ | MAJEUR | E02 (emails.csv), api/newsletter.js | Email de confirmation, double opt-in, désinscription | Email de confirmation avec lien (double opt-in) ; lien de désinscription ; impossibilité d'inscrire l'adresse d'un tiers | Aucun email n'est émis (README ligne 55 : "No newsletter email is sent by this endpoint" ; `api/newsletter.js` n'appelle que `https://api.resend.com/contacts`, ligne 23) ; le contact est créé directement abonné (`{email, unsubscribed:false}` ligne 30) et un contact désinscrit est réabonné par `PATCH {unsubscribed:false}` (ligne 28), attribut que la documentation Resend décrit comme "The Contact’s global subscription status" ; n'importe qui peut donc inscrire ou réabonner n'importe quelle adresse sans vérification, et le site n'offre aucun mécanisme de désinscription | `api/newsletter.js` lignes 23-35 ; README.md ligne 55 ; doc Resend update-contact (scratchpad/flux01/resend-update-contact.html, Body Parameters "unsubscribed boolean The Contact’s global subscription status. If set to true, the contact will be unsubscribed from all Broadcasts") | C4 |
| DÉGRADÉ | MINEUR | api/newsletter.js | Paramètre `lang` | Langue enregistrée pour segmenter les envois FR et EN | `lang` est validé (ligne 20 : refus si différent de fr/en) mais jamais transmis à Resend : les lignes 28, 30 et 33 n'envoient que `email` et `unsubscribed` ; un abonné inscrit depuis une page FR est indistinguable d'un abonné EN | `api/newsletter.js` lignes 20, 28, 30, 33 ; corps réellement envoyé par le navigateur contenant `"lang":"fr"` (submit.json) | C5 |
| OK | n/a | 26 pages : /, /index-fr, /our-solution, /notre-solution, /pricing, /tarifs, /faq, /faq-fr, /about, /a-propos, /careers, /recrutement, /contact, /contact-fr, /customers, /clients, /media, /medias, /legal-notice, /mentions-legales, /privacy, /confidentialite, /terms, /conditions, /opt-out, /opposition | Présence et structure du bloc : `<section id="newsletter" aria-labelledby="newsletter-title">` dans `<footer>`, h2 "Restez informé" / "Stay in the loop", promesse "Recevez nos nouveautés produit et annonces importantes." / "Get product updates and important announcements.", `<form method="post" action="/api/newsletter" aria-label="Newsletter">`, champs email (email, requis), submit, company_website (text) | Identique au générateur `scripts/site_layout.py` lignes 81-88 | Conforme sur 26/26 pages en production (HTTP 200 chacune, y compris /medias qui avait expiré en phase 0 : 200 en 0,16 s à 19:37Z) ; 13 pages EN et 13 FR avec les textes attendus ; formulaire visible (480x44 px à 1440, dans le fenêtrage après défilement ; pleine largeur à 390) ; un seul formulaire par page | scratchpad/flux01/pages.json et pages.log (26 lignes "forms=1") ; `curl -o /dev/null -w '%{http_code}' https://www.syntheticswarm.ai/medias` : 200 ; captures docs/audit-total/screens/01/index-fr-1440-newsletter.png, index-fr-390-newsletter.png, pricing-1440-newsletter.png, opposition-1440-newsletter.png, opposition-390-newsletter.png | Aucune |
| OK | n/a | 26 pages | Chargement de `newsletter.js` (fichiers.csv : newsletter.js, api/newsletter.js) | Une balise, une requête réseau, gestionnaire attaché ; source serveur non exposée | 1 balise `<script src="/newsletter.js" defer>` et 1 requête réseau 200 par page sur 26/26 ; gestionnaire actif sur 26/26 (soumission de contrôle interceptée dans le navigateur : message d'erreur affiché puis libellé rétabli) ; fichier servi identique au repo (sha256 36306300f0c7b6af6b60018e49cfb86e0aa17249c7cec9950bbba200b0f001db, 1 890 octets, `application/javascript; charset=utf-8`) ; `api/newsletter.js` non servi en source (fichiers.csv : 308 vers /api/newsletter) | pages.json (scriptTags = 1, jsReqs = 1, jsAttach.status renseigné sur 26 pages) ; `curl -D - https://www.syntheticswarm.ai/newsletter.js` + `sha256sum` ; fichiers.csv ligne api/newsletter.js | Aucune |
| OK | n/a | 26 pages (index-fr, pricing, opposition pour axe) | Label accessible du champ email (actions.csv input_email "Votre email" / "Your email") | Nom accessible, attributs de saisie corrects, contraste AA | `aria-label="Votre email"` / `"Your email"` (égal au placeholder), `type="email" required maxlength="254" autocomplete="email"` ; axe scope `#newsletter` : 0 violation sur les 3 pages (règles label, button-name, aria-*, autocomplete-valid, landmark-unique passées) ; contraste calculé du texte #666666 sur le fond effectif #fefdfc (blanc à 55 % sur #fcfbf8) : 5,65:1, bouton 5,55:1, placeholder Chromium #757575 : 4,54:1, AA respecté (axe classe color-contrast en "incomplete" à cause du fond translucide) | pages.json (input.ariaLabel, axe.violations = []) ; calcul WCAG (formule de luminance relative) sur les couleurs lues par getComputedStyle : `rgb(102, 102, 102)` sur `rgba(255, 255, 255, 0.55)` et `body { background-color: #fcfbf8 }` (styles.css ligne 13) | Aucune |
| DÉGRADÉ | MINEUR | 26 pages | Label visible du champ email | Étiquette visible persistante (WCAG 3.3.2, bonne pratique formulaires) | Aucun élément `<label>` (input.labels.length = 0, pas d'attribut id) ; le placeholder est la seule indication visible et disparaît dès la saisie | pages.json (input.nbLabelElements = 0, input.id = "") ; `scripts/site_layout.py` ligne 85 | `scripts/site_layout.py` ligne 85 : ajouter `id="newsletter-email"` sur l'input et, juste avant, `<label for="newsletter-email" class="newsletter-label">{'Votre email' if fr else 'Your email'}</label>` ; site-pages.css après la ligne 191 : `.newsletter-label { display:block; margin:0 0 6px; font-size:12px; color:#666; }` ; supprimer l'attribut aria-label devenu redondant ; régénérer avec `python3 scripts/site_layout.py` |
| OK | n/a | 26 pages | Pot de miel `company_website` (actions.csv input_text) | Invisible, hors tabulation, ignoré par les lecteurs d'écran, court-circuit serveur silencieux | `type="text" tabindex="-1" aria-hidden="true" autocomplete="off"`, rendu 1x1 px, `position:absolute; clip-path:inset(50%)` (site-pages.css ligne 189) ; côté serveur, toute valeur non vide renvoie 200 `{"ok":true}` sans appel Resend (`api/newsletter.js` ligne 16, test ligne 16). Le "visible_1440 = oui" d'actions.csv est un faux positif de la règle d'inventaire (clip-path non pris en compte) | pages.json (honeypot.rect w = 1, h = 1, clipPath = inset(50%), position = absolute sur 26 pages) | Aucune |
| OK | n/a | 26 pages | Bouton "S’inscrire" / "Subscribe" (actions.csv button submit) | `type="submit"`, actif, libellés d'état cohérents | Présent et actif sur 26/26 (87x44 px FR, 92x44 px EN) ; pendant la requête : "Inscription…", `disabled`, formulaire `aria-busy="true"` ; après erreur : libellé d'origine rétabli ; après succès (simulé 200) : "Inscription confirmée." puis libellé rétabli à la saisie suivante (écouteur input, `newsletter.js` ligne 13) | submit.json pendingStates (button = "Inscription…", disabled = true, ariaBusy = "true") ; recapture.json (succesSimule.button = "Inscription confirmée.", apresSaisie.button = "S’inscrire") ; captures index-fr-1440-newsletter-pendant.png, index-fr-1440-newsletter-pendant-rejeu503.png, index-fr-1440-newsletter-succes-simule.png | Aucune |
| OK | n/a | 26 pages | Message d'état | Zone live, masquée si vide, textes FR/EN | `<p data-newsletter-status role="status" aria-live="polite" aria-atomic="true">`, `display:none` quand vide et `block` avec texte (12 px, site-pages.css lignes 190-191) ; textes conformes à `newsletter.js` lignes 7-11 ("Inscription confirmée." / "Impossible de vous inscrire pour le moment. Réessayez." et équivalents EN) | pages.json (status.role, ariaLive, ariaAtomic, displayQuandVide = none sur 26 pages) ; submit.json apres1 (statusDisplay = block) ; capture index-fr-1440-newsletter-apres-rejeu503.png | Aucune |
| DÉGRADÉ | MINEUR | 26 pages | Focus clavier pendant et après la soumission | Focus conservé sur le bouton ou déplacé vers le message | Le bouton est désactivé alors qu'il porte le focus (`newsletter.js` ligne 18 `button.disabled = true`) : le focus tombe sur `<body>` pendant la requête et y reste après la réponse ; un utilisateur clavier ou de lecteur d'écran perd sa position dans la page | submit.json (pendingStates[0].focus = "body", apres1.focus = "body") ; recapture.json (pendantRejeu.focus = "body", apresRejeu503.focus = "body") | C6 |
| OK | n/a | /index-fr (Chromium, WebKit, Firefox) | Validation navigateur : email invalide et champ vide | Blocage natif, aucune requête, focus sur le champ | Email "AUDIT TEST 2026-09-19T19:35Z" : typeMismatch, messages natifs Chromium "Please include an '@' in the email address. 'AUDIT TEST 2026-09-19T19:35Z' is missing an '@'.", WebKit "Enter an email address", Firefox "Please enter an email address." ; champ vide : valueMissing, "Please fill out this field." (Chromium, Firefox), "Fill out this field" (WebKit) ; 0 requête vers /api/newsletter dans les 3 navigateurs ; focus placé sur le champ ; le message d'état reste vide. La langue des messages natifs suit la locale du navigateur (anglais dans les binaires Playwright), pas `lang="fr"` ; la bulle native n'est pas rendue dans les captures headless (message lu via `validationMessage`) | scratchpad/flux01/variants.json (requetesInterceptees = [] pour les 3 navigateurs) ; captures index-fr-1440-newsletter-email-invalide.png, index-fr-1440-newsletter-champ-vide.png | Aucune |
| DÉGRADÉ | MINEUR | 26 pages | Adresse acceptée par le navigateur mais refusée par le serveur (ex. `a@b`) | Message explicite d'adresse invalide | Les 3 navigateurs acceptent `a@b` (checkValidity = true) ; le serveur répond 400 (regex ligne 19 exigeant un point dans le domaine, test ligne 12) et le client affiche le message générique "Impossible de vous inscrire pour le moment. Réessayez." qui invite à réessayer une adresse qui sera toujours refusée | variants.json (sansPoint.checkValidity = true dans les 3 navigateurs) ; simul.json apres400 (réponse 400 fabriquée localement : status = "Impossible de vous inscrire pour le moment. Réessayez.") | C7 |
| OK | n/a | newsletter.js, api/newsletter.js | Timeout client 25 s et gestion des erreurs | Abandon à 25 s, message d'erreur, bouton rétabli, aria-busy retiré | Requête retenue dans le navigateur : message d'erreur après 25,2 s (26,6 s au premier essai), bouton "S’inscrire" réactivé, `aria-busy` retiré ; réponses 502, 400 et non JSON fabriquées : même message et même retour à l'état initial. Côté serveur : `AbortSignal.timeout(10000)` par appel Resend (ligne 25), jusqu'à 3 appels (30 s) contre une durée maximale Vercel de 10 à 15 s selon le plan [donnée à fournir : plan Vercel du projet] : au pire un 504 Vercel, traité comme une erreur par le client | recapture.json (timeoutMs = 25221, apresTimeout.button = "S’inscrire", apres502.status) ; simul.json (timeoutMs = 26640, apresHtml, apres502) ; captures index-fr-1440-newsletter-timeout25s-simule.png, index-fr-1440-newsletter-erreur502-simule.png | Aucune |
| OK | n/a | newsletter.js | Protection contre le double clic | Une seule requête par soumission | Drapeau `busy` (ligne 16) et bouton désactivé : 3 clics pendant une requête retenue = 1 requête émise | simul.json (requetesPendantBusy = 1, etatBusy.button = "Inscription…") | Aucune |
| OK | n/a | tiers.csv (26 pages) | Domaines tiers contactés par le bloc | Aucun tiers côté navigateur, clé API jamais exposée | La soumission n'appelle que `/api/newsletter` sur la même origine (`newsletter.js` ligne 25 ; hôtes contactés après le clic en simulation = ["127.0.0.1:8787"] uniquement) ; Resend (api.resend.com) n'est appelé que côté serveur (`api/newsletter.js` ligne 23) avec la clé lue dans `process.env` (ligne 24) | simul.json (hotesContactesApresClic) ; submit.json (une seule URL de requête) ; pages.json (aucune ressource tierce liée au bloc) | Aucune |
| DÉGRADÉ | MINEUR | fichiers.csv (tests/newsletter.test.cjs) | Fichier de test servi en production | Fichiers de test non publiés | https://www.syntheticswarm.ai/tests/newsletter.test.cjs répond 200 (4 934 octets, `application/node`) : le code de test (attentes, mocks, clés factices) est public ; sans impact fonctionnel | `curl -I https://www.syntheticswarm.ai/tests/newsletter.test.cjs` : `HTTP/2 200` ; fichiers.csv ligne tests/newsletter.test.cjs (code_http_prod 200) | C8 |
| OK | n/a | README.md, CONTACT_SETUP.md | Documentation contre réalité | Documentation exacte | README ligne 55 décrit exactement le code (JSON, plafond 2 Ko, pot de miel, PATCH puis POST, aucun email envoyé) et exige "Configure RESEND_API_KEY with Contacts permissions on Vercel" ; CONTACT_SETUP.md lignes 11-12 prévient que sans clé les soumissions renvoient 503 : c'est exactement l'état constaté en production | README.md ligne 55 ; CONTACT_SETUP.md lignes 8-12 | Aucune (C1 applique la documentation) |
| OK | n/a | api/newsletter.js | Validations serveur | Refus des méthodes, types et corps invalides sans fuite d'information | GET → 405 avec `Allow: POST` (ligne 5) ; type non JSON → 415 (ligne 6, observé en production lors de l'envoi n° 3) ; corps > 2 048 octets → 413 (ligne 11) ; email absent, > 320 caractères ou hors regex → 400 (lignes 17-19) ; `lang` hors fr/en → 400 (ligne 20) ; échec ou réponse Resend sans id → 502 sans relayer le message du fournisseur (lignes 37-39) ; réponse toujours `{"ok":...}` avec `Cache-Control: no-store` (ligne 3) ; 5/5 tests unitaires passent | `tests/newsletter.test.cjs` lignes 10-17 et 38-42 ; nojs.json (415 réel) ; sortie `node --test tests/newsletter.test.cjs` (copie du repo) : "# tests 5 / # pass 5 / # fail 0" | Aucune |

## Envois réels vers /api/newsletter (3 au total, plafond respecté)

| N° | Horodatage UTC (requête / réponse) | Adresse | Page | Mode | Code | Corps de réponse | x-vercel-id |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-19T19:51:53.981Z / 19:51:56.700Z | contact+audit-nl-20260919T195150Z@syntheticswarm.ai | /index-fr | JSON via newsletter.js (Chromium 141) | 503 | `{"ok":false}` | iad1::iad1::cnrw6-1789847516058-3171777dfa27 |
| 2 | 2026-09-19T19:52:01.121Z / 19:52:04.399Z | contact+audit-nl-20260919T195150Z@syntheticswarm.ai (même adresse, double soumission) | /index-fr | JSON via newsletter.js | 503 | `{"ok":false}` | iad1::iad1::bfp7p-1789847524215-b54e616e7a96 |
| 3 | 2026-09-19T19:55:30.580Z / 19:55:34.093Z | contact+audit-nl-nojs-20260919T195522Z@syntheticswarm.ai | /index-fr | application/x-www-form-urlencoded, JavaScript désactivé | 415 | `{"ok":false}` | iad1::iad1::q44t9-1789847733716-9bb2254bee65 |

Le signe plus des adresses n'a pas été rejeté (le serveur n'a jamais atteint le fournisseur). Aucune adresse n'a été enregistrée chez Resend (503 avant tout appel ; 415 avant lecture du corps). Toutes les autres soumissions des scripts (26 soumissions de contrôle de nl01-pages, 6 de nl01-simul, 5 de nl01-recapture, 0 émise par nl01-variants) ont été retenues, annulées ou satisfaites dans le navigateur par `page.route` et n'ont jamais quitté le navigateur (preuves : pages.json `apiReqsInterceptees`, recapture.json `controle` = "requetes vues=5 interceptees=5").

## Corrections détaillées

C1 (TROMPEUR BLOQUANT, clé Resend absente) : dans le projet Vercel, créer une clé Resend avec la permission "Full access" (nécessaire pour l'API Contacts, une clé "Sending access" ne suffit pas) sur https://resend.com/api-keys, puis :
```
vercel env add RESEND_API_KEY production
vercel --prod
curl -s -o /dev/null -w '%{http_code}\n' -X POST https://www.syntheticswarm.ai/api/newsletter -H 'Content-Type: application/json' -d '{"email":"contact+verif@syntheticswarm.ai","lang":"fr"}'
```
Résultat attendu de la vérification : `200`, et le contact `contact+verif@syntheticswarm.ai` visible dans Resend > Contacts. Tant que la clé n'est pas posée, ne pas afficher le bloc : supprimer les lignes 81 à 89 de `scripts/site_layout.py` (section `#newsletter`) et régénérer les pages avec `python3 scripts/site_layout.py`, ou ajouter une alerte sur les réponses 503 de `/api/newsletter` dans les logs Vercel.

C2 (sans JavaScript) : dans `api/newsletter.js`, remplacer la ligne 6 par :
```
const type = req.headers['content-type'] || '';
const isForm = /^application\/x-www-form-urlencoded(?:;|$)/i.test(type);
if (!isForm && !/^application\/json(?:;|$)/i.test(type)) return reply(415);
```
remplacer la ligne 12 `body = JSON.parse(raw);` par :
```
body = isForm && typeof req.body === 'string' ? Object.fromEntries(new URLSearchParams(raw)) : JSON.parse(raw);
```
et remplacer la ligne 4 par une réponse HTML lisible pour les envois de formulaire :
```
const back = /^\/[a-z0-9-]*$/i.test(req.headers.referer ? new URL(req.headers.referer, 'https://www.syntheticswarm.ai').pathname : '') ? new URL(req.headers.referer, 'https://www.syntheticswarm.ai').pathname : '/';
const reply = (status, ok = false) => {
  if (!/^application\/x-www-form-urlencoded/i.test(req.headers['content-type'] || '')) return res.status(status).json({ok});
  const fr = (req.headers['accept-language'] || '').toLowerCase().startsWith('fr');
  res.setHeader('Content-Type', 'text/html; charset=utf-8');
  return res.status(ok ? 200 : status).send(`<!doctype html><html lang="${fr ? 'fr' : 'en'}"><meta charset="utf-8"><title>Newsletter</title><p>${ok ? (fr ? 'Inscription confirmée.' : 'You’re subscribed.') : (fr ? 'Impossible de vous inscrire pour le moment. Réessayez.' : 'Unable to subscribe right now. Please try again.')}</p><p><a href="${back}#newsletter">${fr ? 'Retour' : 'Back'}</a></p></html>`);
};
```
Ajouter au test `tests/newsletter.test.cjs` un cas `call('email=a%40example.com','POST',{'content-type':'application/x-www-form-urlencoded'})` attendu 200.

C3 (information RGPD) : dans `scripts/site_layout.py`, après la ligne 87 (`<p data-newsletter-status ...></p>`), ajouter :
```
            <p class="newsletter-legal">{'En vous inscrivant, vous acceptez de recevoir par email nos nouveautés produit et annonces (base légale : votre consentement). Désinscription à tout moment via le lien présent dans chaque email ou en écrivant à contact@syntheticswarm.ai. <a href="/confidentialite">Politique de confidentialité</a>.' if fr else 'By subscribing you agree to receive our product updates and announcements by email (legal basis: your consent). Unsubscribe at any time using the link in every email or by writing to contact@syntheticswarm.ai. <a href="/privacy">Privacy policy</a>.'}</p>
```
dans `site-pages.css` après la ligne 191 : `.brand-close-newsletter .newsletter-legal { margin:10px 0 0; font-size:11px; line-height:1.5; color:#666; } .brand-close-newsletter .newsletter-legal a { text-decoration:underline; }` ; régénérer avec `python3 scripts/site_layout.py`. Dans `confidentialite.html`, ajouter dans la liste des traitements : "Newsletter : lorsque vous saisissez votre email dans le bloc « Restez informé », nous l'enregistrons dans notre outil d'emailing Resend (Resend Inc., États-Unis ; transfert hors UE encadré par [donnée à fournir : mécanisme retenu, par exemple clauses contractuelles types]) afin de vous envoyer nos nouveautés produit et annonces. Base légale : votre consentement. Durée : jusqu'à votre désinscription, puis suppression sous 30 jours. Vous pouvez vous désinscrire via le lien présent dans chaque email ou en écrivant à contact@syntheticswarm.ai." Dans `privacy.html` : "Newsletter: when you enter your email in the “Stay in the loop” block, we store it in our emailing tool Resend (Resend Inc., United States; transfer outside the EU covered by [donnée à fournir : mechanism, for example standard contractual clauses]) to send you product updates and announcements. Legal basis: your consent. Retention: until you unsubscribe, then deletion within 30 days. You can unsubscribe using the link in every email or by writing to contact@syntheticswarm.ai."

C4 (double opt-in) : dans `api/newsletter.js`, lignes 28, 30 et 33, créer ou mettre à jour le contact avec `unsubscribed:true` (au lieu de `false`), puis, après la ligne 37, envoyer l'email de confirmation :
```
const token = require('node:crypto').createHmac('sha256', process.env.NEWSLETTER_SECRET).update(email).digest('hex');
const confirm = `https://www.syntheticswarm.ai/api/newsletter-confirm?email=${encodeURIComponent(email)}&token=${token}`;
const mail = await fetch('https://api.resend.com/emails', {method:'POST', headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`, 'Content-Type':'application/json'}, signal: AbortSignal.timeout(10000),
  body: JSON.stringify({from: process.env.CONTACT_FROM_EMAIL, to: email, subject: body.lang === 'fr' ? 'Confirmez votre inscription à la newsletter Synthetic Swarm' : 'Confirm your Synthetic Swarm newsletter subscription',
    text: body.lang === 'fr' ? `Pour confirmer votre inscription, ouvrez ce lien : ${confirm}\nSi vous n'êtes pas à l'origine de cette demande, ignorez cet email.` : `To confirm your subscription, open this link: ${confirm}\nIf you did not request this, ignore this email.`})});
if (!mail.ok) return reply(502);
```
Créer `api/newsletter-confirm.js` qui vérifie le HMAC (`crypto.timingSafeEqual`), envoie `PATCH https://api.resend.com/contacts/{email}` avec `{unsubscribed:false}` et redirige en 303 vers `/index-fr#newsletter` (ou `/#newsletter`). Ajouter `NEWSLETTER_SECRET` et `CONTACT_FROM_EMAIL` aux variables Vercel. Dans `newsletter.js` ligne 8, remplacer `success:'Inscription confirmée.'` par `success:'Vérifiez votre boîte mail pour confirmer votre inscription.'` et ligne 10 `success:'You’re subscribed.'` par `success:'Check your inbox to confirm your subscription.'`. Inclure `{{{RESEND_UNSUBSCRIBE_URL}}}` dans chaque diffusion Resend.

C5 (langue) : dans `api/newsletter.js`, remplacer la ligne 28 par `let result = await request(`/${encodeURIComponent(email)}`, 'PATCH', {unsubscribed:false, properties:{lang: body.lang || 'en'}});`, la ligne 30 par `result = await request('', 'POST', {email, unsubscribed:false, properties:{lang: body.lang || 'en'}});` et la ligne 33 de la même façon ; créer au préalable la propriété personnalisée `lang` (type texte) dans Resend > Contacts > Properties, et adapter l'assertion de `tests/newsletter.test.cjs` ligne 31 en `{email:'test@example.com',unsubscribed:false,properties:{lang:'fr'}}`.

C6 (focus) : dans `newsletter.js`, remplacer la ligne 18 `button.disabled = true;` par `button.setAttribute('aria-disabled', 'true');`, la ligne 39 `button.disabled = false;` par `button.removeAttribute('aria-disabled');` (le drapeau `busy` de la ligne 16 empêche déjà les envois multiples) et adapter `tests/newsletter.test.cjs` lignes 64-71 (`button.getAttribute('aria-disabled')` à la place de `button.disabled`).

C7 (adresse refusée par le serveur) : dans `newsletter.js`, ajouter `invalid:'Adresse email invalide.'` à l'objet FR (ligne 8) et `invalid:'Invalid email address.'` à l'objet EN (ligne 10), puis remplacer la ligne 30 par :
```
      if (response.status === 400) { status.textContent = copy.invalid; button.textContent = label; return; }
      if (!response.ok || result.ok !== true) throw new Error('subscription failed');
```

C8 (fichier de test public) : créer `.vercelignore` à la racine du repo avec le contenu :
```
tests
```
puis redéployer ; vérification : `curl -o /dev/null -w '%{http_code}\n' https://www.syntheticswarm.ai/tests/newsletter.test.cjs` attendu `404`.

## Méthode et garde-fous

Toutes les mesures ont été faites sur https://www.syntheticswarm.ai (commit 864833f) sauf les simulations locales (http://127.0.0.1:8787, copie servie du même commit, Chromium sans proxy car le proxy refuse les requêtes HTTP non CONNECT). Les scripts nl01-pages, nl01-variants, nl01-simul et nl01-recapture interceptent `**/api/newsletter` avec `page.route` et n'appellent jamais `route.continue()` ; nl01-submit et nl01-nojs comportent un compteur qui annule toute requête au-delà de 2 et de 1 respectivement. Le serveur local 127.0.0.1:8787 n'a pas été utilisé pour tester l'API (stub). Les tests unitaires ont été exécutés sur une copie du repo dans le scratchpad (`git status` du repo : aucun fichier modifié hors docs/audit-total). Les captures 1440 de plus de 400 Ko ont été réduites à 60 % (864x540). Les captures "-rejeu503" reproduisent dans le navigateur la réponse 503 réelle (mêmes code, en-têtes `cache-control` et `content-type`, même corps) uniquement pour cadrer le message affiché ; les captures "-simule" correspondent à des réponses fabriquées (200, 502) ou retenues (timeout).

## Compte

Lignes du tableau : 22.
Par verdict : OK 12 ; TROMPEUR 1 ; DÉGRADÉ 8 ; NON TESTABLE 1 ; CASSÉ 0.
Par sévérité : BLOQUANT 2 (TROMPEUR clé Resend absente ; DÉGRADÉ information RGPD absente) ; MAJEUR 2 (soumission sans JavaScript ; absence de confirmation et de double opt-in) ; MINEUR 5 (langue non transmise ; label visible absent ; perte du focus ; message générique sur 400 ; fichier de test public) ; n/a 13 (12 OK, 1 NON TESTABLE).
Envois réels : 3 (503, 503, 415). Captures : 18 fichiers dans docs/audit-total/screens/01/, toutes sous 400 Ko.

## Couverture

- pages.csv : 26/26 URL, bloc newsletter contrôlé sur chacune (ligne OK "Présence et structure du bloc" et ligne OK "Chargement de newsletter.js"), verdict TROMPEUR BLOQUANT applicable aux 26 formulaires.
- actions.csv, région footer, 104 lignes : 26 `form` "Newsletter" (`/api/newsletter`) ; 26 `input_email` "Votre email" / "Your email" ; 26 `button` "S’inscrire" / "Subscribe" ; 26 `input_text` `company_website` (pot de miel). Chaque type reçoit une ligne dédiée du tableau, plus les lignes de soumission (envois réels 1 à 3, validation navigateur, timeout, double clic).
- emails.csv : E02 (aucun email émis par /api/newsletter) : ligne DÉGRADÉ MAJEUR "Email de confirmation, double opt-in, désinscription".
- fichiers.csv : `newsletter.js` (servi 200, identique au repo), `api/newsletter.js` (source non exposée, 308 ; comportement 503/415 constaté), `tests/newsletter.test.cjs` (servi 200 : ligne DÉGRADÉ MINEUR).
- tiers.csv : aucun domaine tiers contacté par le bloc côté navigateur (ligne OK "Domaines tiers contactés par le bloc") ; Resend n'est joint que côté serveur.
- Code lu intégralement : `api/newsletter.js` (40 lignes), `newsletter.js` (43 lignes), `scripts/site_layout.py` (générateur du pied de page et injection du script), README.md section "Homepage newsletter", CONTACT_SETUP.md, `tests/newsletter.test.cjs` (72 lignes), site-pages.css lignes 152-191, documentation Resend Contacts (create-contact, update-contact).
